<Window x:Class="RestaurantApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:RestaurantApp.Core"
        xmlns:viewmodels="clr-namespace:RestaurantApp.ViewModels"
        x:Name="thisWindow"
        Title="Login" Height="350" Width="400" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewmodels:LoginViewModel />
    </Window.DataContext>

    <StackPanel Margin="30">
        <TextBlock Text="Email:"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <TextBlock Text="Parolă:"/>
        <PasswordBox core:PasswordHelper.Password="{Binding Parola, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,5"/>

        <TextBlock Text="{Binding MesajEroare}" Foreground="Red"/>

        <Button Content="Login" 
                Command="{Binding LoginCommand}" 
                CommandParameter="{Binding ElementName=thisWindow}" 
                Margin="0,10" Padding="5"/>

        <Button Content="Continuă fără cont" 
                Command="{Binding ContinuaFaraContCommand}" 
                CommandParameter="{Binding ElementName=thisWindow}"
                Background="Transparent" BorderThickness="0" Foreground="Blue"/>
    </StackPanel>
</Window>